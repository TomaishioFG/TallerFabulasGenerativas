<resources>
    <!-- Estilo para RadioButton -->
    <style name="CustomRadioButton" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <item name="android:background">@drawable/custom_button</item>
        <item name="android:textSize">18sp</item> <!-- Reduced text size -->
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item> <!-- Center the text -->
        <item name="android:padding">16dp</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="CustomButton" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <item name="android:gravity">center</item> <!-- Center the text -->
        <item name="android:background">@drawable/custom_button</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textSize">20sp</item>
        <item name="android:padding">16dp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@drawable/splash</item>
    </style>

    <!-- Estilo para CheckBox -->
    <style name="CustomCheckBox" parent="Widget.AppCompat.CompoundButton.CheckBox">
        <item name="android:background">@drawable/custom_button</item>
        <item name="android:textSize">18sp</item> <!-- Reduced text size -->
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item> <!-- Center the text -->
        <item name="android:padding">16dp</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>
</resources>